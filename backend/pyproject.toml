[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.110",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "uvicorn[standard]>=0.27",
]
